{ config, pkgs, inputs, ... }:

{
  imports = [
    ./modules/hyprland.nix
  ];
  home.username = "zhenya";
  home.homeDirectory = "/home/zhenya";
  home.stateVersion = "25.05";

 # programs.bash = {
 #   enable = true;
 #   shellAliases = {
 #     btw = "what";
 #   };
 # };
  programs.home-manager.enable = true;

  home.packages = with pkgs; [

  ];
  
}
